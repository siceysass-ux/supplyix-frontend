import React from 'react';

// This component is no longer used as notifications are now handled in a popup within the DashboardHeader.
// It is kept empty to avoid breaking any potential old imports, but it should be considered deprecated.
const NotificationsPage: React.FC = () => {
    return null;
};

export default NotificationsPage;