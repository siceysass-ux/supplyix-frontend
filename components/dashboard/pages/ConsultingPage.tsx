import React from 'react';

// This component is no longer used as its functionality has been merged into the RequestsPage.
// It is kept empty to avoid breaking any potential old imports, but it should be considered deprecated.
const ConsultingPage: React.FC = () => {
    return null;
};

export default ConsultingPage;